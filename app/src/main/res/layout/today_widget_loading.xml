<?xml version="1.0" encoding="utf-8"?>
<!--
    Shown only for the brief instant before Glance's own first composition
    runs. TodayWidgetPublisher.start() and TodayWidgetReceiver's
    onUpdate/onEnabled hooks republish real data as soon as a vault is
    active, but this still renders first for that instant, and for a
    widget placed while no vault is active until one becomes active.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
